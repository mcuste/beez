[package]
name = "loom-daemon"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
loom-manifest = { path = "../loom-manifest" }
loom-record = { path = "../loom-record" }
loom-runner = { path = "../loom-runner" }
loom-sandbox = { path = "../loom-sandbox" }
loom-schedule = { path = "../loom-schedule" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.151"
signal-hook = { version = "0.4.4", default-features = false, features = ["iterator"] }

[dev-dependencies]
loom-test-support = { workspace = true }

[lints]
workspace = true
