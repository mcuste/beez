[package]
name = "beez-sandbox"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Sandbox for beez: Seatbelt on macOS, bubblewrap and Landlock on Linux, and the network proxies"

[dependencies]
beez-policy.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4.7"

[dev-dependencies]
beez-test-support.workspace = true

[lints]
workspace = true
